#include <stdio.h>

int main()
{
    char name[25];
    int type;
    int number;
    int type2;
    
    printf("Enter your name:");
    scanf("%s", &name);
    printf("Enter type of number to convert\n");
    printf("type 1 for binary\ntype 2 for octal\ntype 3 for decimal\ntype 4 for hexa\ntype:");
    scanf("%d", &type);
    printf("Enter to what type of number will it be converted to\n");
    printf("type 1 for binary\ntype 2 for octal\ntype 3 for decimal\ntype 4 for hexa\ntype:");
    scanf("%d", &type2);
    printf("Enter number to convert");
    scanf("%d", &number);

    if (type && type2 ==)
    {



    }



    

    return 0;
}